<Window x:Class="TDNHCS.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đang tải bản cập nhật" Height="180" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Đang tải bản cập nhật..."
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="#2C3E50"/>

        <ProgressBar Grid.Row="1"
                     x:Name="progressBar"
                     Height="22"
                     Margin="0,16,0,8"
                     Minimum="0"
                     Maximum="100"/>

        <TextBlock Grid.Row="2"
                   x:Name="txtStatus"
                   Text="0%"
                   HorizontalAlignment="Center"
                   Foreground="#566573"/>
    </Grid>
</Window>
